# Environment variables
.env*.local

# Common ignores
node_modules
.next/
.DS_Store
out

# Build-emitted version stamp (scripts/gen-version.mjs, prebuild hook)
public/version.json

# Deploy creds (SSH key path + server) — never commit
deploy.config.sh

# Test artifacts
playwright-report/
test-results/
e2e/.auth/
coverage/